import { IPolygonQuery } from "../transport/request";
import { ITick } from "./ITickJson";
export interface IHistoricTradeQuery extends IPolygonQuery {
    offset?: number;
    limit?: number;
}
export interface IHistoricTrade {
    day?: string;
    msLatency?: number;
    status?: string;
    symbol?: string;
    ticks?: ITick[];
    map?: {
        [key: string]: string;
    };
}
export declare const historicTrades: (apiKey: string, apiBase: string, from: string, to: string, date: string, query?: IHistoricTradeQuery) => Promise<IHistoricTrade>;
