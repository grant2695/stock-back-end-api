import { ITradesQuotesQuery, ITradeInfo } from "./trades";
export interface IQuotes {
    db_latency?: number;
    results_count?: number;
    success?: boolean;
    ticker?: string;
    results?: ITradeInfo[];
}
export declare const quotes: (apiKey: string, apiBase: string, symbol: string, date: string, query?: ITradesQuotesQuery) => Promise<IQuotes>;
